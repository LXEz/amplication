import * as React from "react";

import {
  Create,
  SimpleForm,
  CreateProps,
  TextInput,
  ReferenceArrayInput,
  SelectArrayInput,
} from "react-admin";

import { ToDoTaskTitle } from "../toDoTask/ToDoTaskTitle";

export const ToDoUserCreate = (props: CreateProps): React.ReactElement => {
  return (
    <Create {...props}>
      <SimpleForm>
        <TextInput label="Email" source="email" type="email" />
        <TextInput label="lookup" source="lookup" />
        <TextInput label="Name" source="name" />
        <ReferenceArrayInput source="toDoTasks" reference="ToDoTask">
          <SelectArrayInput
            optionText={ToDoTaskTitle}
            parse={(value: any) => value && value.map((v: any) => ({ id: v }))}
            format={(value: any) => value && value.map((v: any) => v.id)}
          />
        </ReferenceArrayInput>
      </SimpleForm>
    </Create>
  );
};
